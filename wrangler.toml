name = "podcast-graphs-web"
compatibility_date = "2024-01-01"

# Pages configuration
pages_build_output_dir = "site"

# Note: Build configuration for Pages is done in the Cloudflare dashboard,
# not in wrangler.toml. The [build] section is not supported for Pages.

# Environment variables (if needed)
# [vars]
# ENVIRONMENT = "production"
